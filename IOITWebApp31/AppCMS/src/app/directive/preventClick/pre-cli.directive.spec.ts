import { PreCliDirective } from './pre-cli.directive';

describe('PreCliDirective', () => {
  it('should create an instance', () => {
    const directive = new PreCliDirective();
    expect(directive).toBeTruthy();
  });
});
